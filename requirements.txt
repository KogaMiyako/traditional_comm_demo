# Manually curated dependencies for this project.
# This file is intentionally not generated from `conda env export`.
# Tested with Python 3.9 and CUDA-enabled PyTorch 1.13.x.

# Core numerical and image processing
numpy>=1.24,<2
Pillow>=9,<12

# CIFAR-10 training/inference
# Choose the CUDA build appropriate for the target machine when installing
# PyTorch. The version range keeps torch/torchvision compatibility flexible.
torch>=1.13,<2.1
torchvision>=0.14,<0.16
scikit-learn>=1.0,<2

# MMSA LF-DNN training/inference
pandas>=1.5,<3
tqdm>=4.64,<5
easydict>=1.7,<2
pytorch-transformers==1.2.0
transformers>=4,<5

# Optional GPU/environment information used by the training utilities.
nvidia-ml-py3>=7.352.0

# System dependency, not installed by pip:
# ffmpeg
