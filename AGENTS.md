# 传统通信 Demo 实现约束

本文件约束仓库中“内容2：传统通信模型搭建”的后续代码实现。实现依据：

- `doc/任务要求.txt`
- `doc/语义原生的泛在智简无线组网方法_Demo方案.docx`

## 一、任务边界

“内容2”负责传统通信基线，不负责重新实现通信硬件、物理层、语义模型或 Web 页面。

### 内容2负责

- 图片/视频读取
- JPEG、H.264、AV1 等常规编码和解码适配
- 编码数据封装为可发送的字节数据
- 调用课题三的数据收发接口
- 传统通信模式的重建结果或任务结果
- 通信、时延、质量、任务和资源指标的记录
- 向课题一提供控制、状态和结果数据
- 与语义通信共用的运行编号、配置和结果格式

### 不要在内容2中硬编码或重复实现

- 课题三的通信硬件和实际链路
- TCP、UDP、Wi-Fi、5G 或有线链路的具体实现
- SNR 的固定取值
- 具体识别类别或业务场景
- 语义模型、知识库和语义载荷格式
- Web 页面和拓扑可视化
- 设备状态采集代理

如果课题三暂时没有接口，可以使用本地 TCP/UDP 适配器做开发测试，但必须通过统一的 `TransportAdapter` 接口隔离，不能让业务代码依赖临时协议。

## 二、必须预留的接口层

代码至少应按以下职责拆分。具体文件名可以调整，但职责不能消失。

```text
config              # 任务、样本、编码、信道和运行配置
run_control         # 运行启动、状态、取消、完成和错误
codec_adapter       # JPEG/H.264/AV1 等编码和解码
transport_adapter   # 课题三数据收发入口
task_adapter        # 重建或任务模型输出
metrics             # 通信、质量、任务、资源指标
state_collector     # 节点、链路、模型和运行状态
result_store        # 原始日志、结果文件和汇总结果
comparison          # 传统/语义对比和 SNR 曲线
web_adapter         # 提供给课题一的控制和查询接口
```

## 三、统一运行上下文

两种通信模式必须共用同一种运行上下文。每次运行必须有独立的 `run_id`，不能只用时间戳或临时文件名作为唯一标识。

至少预留以下字段：

```yaml
run_id: ""
mode: traditional  # traditional / semantic
sample:
  sample_id: ""
  path: ""
  media_type: image  # image / video
  original_format: ""
  checksum: ""
task:
  task_id: ""
  task_type: ""
  output_type: reconstruction  # reconstruction / structured / task_result
codec:
  name: ""           # jpeg / h264 / av1 等
  container: ""      # jpg / mp4 / mkv 等，可为空
  parameters: {}
channel:
  link_type: ""      # Wi-Fi / 5G / wired 等，由课题三确定
  snr_db: null
  packet_loss_rate: null
  bit_error_rate: null
nodes:
  sender_id: ""
  receiver_id: ""
system:
  model_version: ""
  knowledge_base_version: ""
  software_versions: {}
```

不得把样本、任务、SNR、编码质量或输出类型写死在业务代码中，应通过配置文件或统一控制接口传入。

## 四、运行控制接口

为了接入 Web 页面和现场演示，需要预留统一生命周期：

```text
health_check()       # 发送端、接收端、课题三链路和状态服务是否在线
prepare_run(config)  # 校验配置、样本、模型、链路和输出目录
start_run(context)   # 启动一次运行并返回 run_id
get_status(run_id)   # 查询运行状态和阶段耗时
cancel_run(run_id)   # 支持现场中止
get_result(run_id)   # 获取结果和指标
get_error(run_id)    # 获取可展示的错误信息
```

建议统一运行状态：

```text
offline -> ready -> preparing -> running -> completing -> completed
                                             \-> failed
                                             \-> cancelled
```

运行状态至少要能说明：当前阶段、百分比或帧进度、错误信息、发送端状态、接收端状态和链路状态。

## 五、传统编解码接口

编码模块只负责内容编码/解码，不负责链路传输。

### 编码输入

- 输入文件或帧
- 图片/视频类型
- 编码格式：JPEG、H.264、AV1 等
- 封装格式：JPG、MP4、MKV 等
- 分辨率、帧率、码率或质量参数
- 编码器名称和版本

### 编码输出

- 编码后的字节数据或临时文件
- 编码格式和封装格式
- 编码后字节数
- 编码耗时
- 编码是否成功
- 错误信息

### 解码输出

- 重建图片或视频
- 解码耗时
- 输出分辨率、帧数和帧率
- 是否成功
- 输出文件路径或任务输入

不要通过修改扩展名冒充编码转换。若需要改变编码格式，必须明确执行解码和重新编码，并记录该过程。

## 六、课题三传输适配接口

课题三提供：通信硬件、链路接入、数据收发入口、SNR 设置方式和传输统计结果。内容2只依赖抽象接口，不直接依赖 TCP、UDP 或某种硬件。

### 必须预留的操作

```text
link_health_check(link_config)
configure_link(link_config)
send_payload(run_context, payload, metadata)
receive_payload(run_context)
close_link(run_id)
get_link_status(run_id)
get_transport_stats(run_id)
```

### 发送元数据至少包括

- `run_id`
- `mode`
- `sample_id`
- `task_id`
- `codec`
- `container`
- `payload_size`
- `sequence_id` 或分片序号
- `expected_total_bytes`
- 链路配置引用

### 传输返回结果至少包括

- 是否发送成功
- 接收端是否完整重组
- 实际发送字节数
- 实际接收字节数
- 平均吞吐
- 峰值吞吐
- 传输耗时
- 丢包数或丢包率（如果可提供）
- 误码率（如果可提供）
- 重传次数（如果可提供）
- SNR 实际值
- 链路错误信息

数据量统计必须明确口径：是否包含协议头、分包头、控制信息和重传数据。建议同时保留“编码载荷字节数”和“实际链路总字节数”。

## 七、任务输出接口

Word 方案允许三类输出，代码不能只支持一种：

- 图像/视频重建结果
- 结构化语义结果
- 任务处理结果

传统通信接收端至少应支持以下路径：

```text
常规解码 -> 输出重建图片/视频
常规解码 -> 运行任务模型 -> 输出任务结果
```

任务模块应预留：

- `task_id`
- `task_type`
- `input_path`
- `output_type`
- 结果文件或结构化结果
- 准确率、召回率、F1、mAP 或结果一致性
- 任务推理耗时
- 模型版本

不要在传统通信代码中假设固定识别类别或固定业务场景。

## 八、性能记录接口

每次运行都要保留原始数据，不能只保存页面上的最终平均值。

### 通信指标

- 单次任务总字节数
- 编码载荷字节数
- 实际链路总字节数
- 平均吞吐
- 峰值吞吐
- 视频平均/峰值帧率
- 视频丢帧数

### 时延指标

- 任务启动时间
- 编码开始/结束时间
- 发送开始/结束时间
- 接收开始/结束时间
- 解码开始/结束时间
- 任务处理开始/结束时间
- 接收端结果形成时间
- 端到端时延

端到端时延统一定义为：

```text
结果形成时间 - 任务启动时间
```

### 质量和任务指标

- PSNR
- SSIM
- LPIPS（算法需要时）
- 准确率
- 召回率
- F1
- mAP
- 结果一致性

如果没有重建图片/视频，不要强行计算 PSNR/SSIM，应使用任务指标。

### 信道指标

- SNR
- 丢包率（课题三能够提供时）
- 误码率（课题三能够提供时）
- 重传次数（课题三能够提供时）

### 资源和状态指标

- 发送端 CPU 利用率
- 接收端 CPU 利用率
- 发送端 GPU 利用率
- 接收端 GPU 利用率
- 内存占用
- 显存占用
- 设备运行状态
- 链路运行状态
- 任务运行状态

### 派生指标

数据量降幅统一按以下公式计算：

```text
（传统模式数据量 - 语义模式数据量）÷ 传统模式数据量
```

SNR 对比必须保存完整的 SNR 序列，并生成质量/任务性能随 SNR 变化的折线数据，同时给出数据量和端到端时延。

## 九、结果存储格式

建议每次运行保留一个独立目录，至少包含：

```text
runs/<run_id>/
  config.json          # 输入配置、任务、模式、SNR、编码参数
  events.jsonl         # 生命周期和阶段事件
  transport.json       # 课题三传输统计
  metrics.json         # 指标汇总
  resource.jsonl       # CPU/GPU/内存/显存采样
  input.*              # 可选：输入引用或校验信息
  encoded.*            # 可选：传统编码载荷
  output.*             # 重建结果或任务结果
  error.json           # 失败时的结构化错误
```

两种模式必须输出可比较的结果格式，至少能通过 `run_id`、`mode`、`sample_id`、`task_id` 和链路条件进行关联。

## 十、Web 和拓扑展示需要的数据

课题一负责 Web 集成，但内容2需要提供查询数据。

### 任务控制区

- 可选图片/视频样本
- 任务类型和输出类型
- 通信模式
- 编码参数
- 链路条件和 SNR
- 启动、停止、重试操作
- 当前运行编号

### 拓扑视图区

- 节点 ID
- 节点角色：发送端/接收端/备用/转发
- 在线状态
- 节点间链路状态
- 链路类型

### 节点详情区

点击拓扑节点后，应能查询：

- 设备状态
- 当前模型版本
- 知识库版本
- 当前运行编号
- CPU/GPU 利用率
- 内存/显存占用
- 操作系统和架构
- 驱动、CUDA、TensorRT、深度学习框架版本
- 当前任务状态

### 链路详情区

点击链路后，应能查询：

- SNR
- 平均吞吐
- 峰值吞吐
- 累计传输量
- 丢包率/误码率（可用时）
- 链路状态

### 结果对比区

- 传统模式和语义模式的运行编号
- 输入配置
- SNR 和链路条件
- 数据量
- 吞吐
- 端到端时延
- PSNR/SSIM/LPIPS 或任务指标
- CPU/GPU、内存/显存状态
- 输出文件和任务结果

## 十一、SNR 对比的特殊约束

SNR 对比不是随意改变参数。代码应支持传入一个 SNR 序列，例如：

```yaml
snr_sweep:
  enabled: true
  values_db: []
```

每个 SNR 值都要分别运行传统模式和语义模式，并保持以下内容不变：

- 样本
- 任务
- 编码配置
- 节点角色
- 模型和软件配置
- 统计口径

SNR 取值由联调确定，不要在代码里固定假设具体数列。

## 十二、设备和软件环境兼容性

主演示使用两台 DGX Spark，第三台为备用。DGX Spark 为 ARM64，RTX 主机通常为 x86_64。代码和依赖必须考虑：

- ARM64 和 x86_64 的软件包差异
- 多架构容器镜像或分架构依赖
- 操作系统版本
- NVIDIA 驱动版本
- CUDA 版本
- TensorRT 版本
- 深度学习框架版本
- FFmpeg/GStreamer 版本
- 编解码器是否可用

正式联调前必须生成设备实测清单，不能只依赖预设规格。模型版本、知识库版本和软件版本都要进入运行记录。

## 十三、演示生命周期必须可复现

实现需要覆盖 Word 方案中的现场流程：

1. 检查发送端、接收端、课题三链路和状态采集服务在线。
2. 选择图片/视频样本、任务输出和链路条件。
3. 启动传统通信并保存结果、传输开销、时延和节点状态。
4. 使用同样的输入、任务和链路条件启动语义通信。
5. 通过统一结果格式并列展示两次运行。
6. 如开展 SNR 对比，按预定 SNR 序列重复两种模式并保存曲线数据。

## 十四、实现时的验收标准

代码完成后，应满足：

- 不修改业务代码即可更换 JPEG/H.264/AV1 等编码器。
- 不修改业务代码即可接入课题三的真实链路。
- 不把 TCP/UDP 写死为最终通信协议。
- 每次运行都有独立运行编号。
- 输入配置、模式、SNR、任务结果和系统状态均可追溯。
- 传统和语义模式使用同一结果格式。
- 质量指标、任务指标和资源指标按任务类型选择，不强行填充无意义指标。
- 失败、取消、超时和链路断开都有结构化状态和错误信息。
- ARM64 DGX Spark 和 x86_64 主机的环境差异被记录并可检查。
- Web 页面能够查询任务、拓扑、节点、链路和对比结果所需字段。

